# Business WhatsApp number in E.164 digits, no "+" and no spaces.
NEXT_PUBLIC_WHATSAPP_NUMBER=77000000000

# Primary domain, used for canonical URLs, sitemap and Open Graph metadata.
# Must match the primary domain in Vercel exactly (include www. if www is primary).
NEXT_PUBLIC_SITE_DOMAIN=refella.app
